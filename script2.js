const Typed = window.Typed;



const typedMain = new Typed('#element', {
  strings: ['Design is intelligence made visible'],
  typeSpeed: 100,
  backSpeed: 50,
  backDelay: 2000,
  startDelay: 1000,
  fadeOut: true,
  fadeOutClass: 'typed-fade-out',
  fadeOutDelay: 500,
  showCursor: false,
});

const typedSub = new Typed('#subtext', {
    strings: ['Interaction design isn’t merely a matter of aesthetic choice; rather, it is based on an understanding of users and cognitive principles.'],
    typeSpeed: 100,
    backSpeed: 50,
    backDelay: 2000,
    startDelay: 1000,
    fadeOut: true,
    fadeOutClass: 'typed-fade-out',
    fadeOutDelay: 500,
    showCursor: false,
});

